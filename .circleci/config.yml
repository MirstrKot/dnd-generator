version: 2.0
jobs:
  GetCode:
    docker:
      - image: circleci/php:7.3.1-fpm-stretch-node
    steps:
      - checkout
      - persist_to_workspace:
          root: .
          paths:
            - .
  BuildJS:
    docker:
      - image: circleci/php:7.3.1-fpm-stretch-node
    steps:
      - attach_workspace:
          at: .
      - restore_cache:
          keys:
            - dndgen-cache-{{ checksum "package.json" }}
      - run:
          name: Building JS
          command: |
            cd ./ClientApp
            npm install
            npm run prod
      - save_cache:
          paths:
            - ./public_html/bundles/
          key: dndgen-cache-{{ checksum "package.json" }}
          name: DndGEN Caches
  Deploy:
    docker:
      - image: circleci/php:7.3.1-fpm-stretch-node
    steps:
      - add_ssh_keys:
          fingerprints:
            - $ssh_fprint
      - attach_workspace:
          at: .
      - restore_cache:
          keys:
            - dndgen-cache-{{ checksum "package.json" }}
      - run:
          name: Deploying
          command: |
            rsync \
                -e "ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" \
                -rltvzO \
                --include public_html/bundles \
                --filter=":- .gitignore" \
                --exclude authorized_keys \
                --exclude .git \
                --exclude .circleci \
                . $ssh_login@$remote_host:$remote_path

workflows:
  version: 2
  BaseWorkflow:
    jobs:
      - GetCode
      - BuildJS:
          requires:
            - GetCode
      - Deploy:
          requires:
            - BuildJS
